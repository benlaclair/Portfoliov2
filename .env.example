# Copy to .env.local and fill in. .env.local is gitignored.
# Vercel has these set as project-level env vars; only needed for local dev.

# Formspree form ID — required by src/pages/api/contact.ts (the contact form proxy).
# Without this, the contact form returns 500 locally.
FORMSPREE_ID=your_form_id_here
